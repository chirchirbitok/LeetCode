function solution(A) {
    // write your code in JavaScript (Node.js 8.9.4)
    let arrSize = A.length;
    for(let i =0; i<arrSize; i++){
        if(A[i] % 4 === 0 ){
            let result = A[i]/4;
            
        }
    }
}